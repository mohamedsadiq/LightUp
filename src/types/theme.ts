export type Theme = "light" | "dark";

export interface ThemedStyles {
  popup: React.CSSProperties;
  popupPositioner: React.CSSProperties;
  text: React.CSSProperties;
  error: React.CSSProperties;
  explanation: React.CSSProperties;
  followUpQA: React.CSSProperties;
  followUpQuestion: React.CSSProperties;
  followUpAnswer: React.CSSProperties;
  followUpInputContainer: React.CSSProperties;
  searchContainer: React.CSSProperties;
  input: React.CSSProperties;
  searchSendButton: React.CSSProperties;
  buttonContainerParent: React.CSSProperties;
  buttonContainer: React.CSSProperties;
  button: React.CSSProperties;
} 